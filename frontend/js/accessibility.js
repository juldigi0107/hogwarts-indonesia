const motionReduced=()=>matchMedia('(prefers-reduced-motion: reduce)').matches||document.documentElement.dataset.motion==='reduce';
export function installAccessibility(){const reduced=matchMedia('(prefers-reduced-motion: reduce)');let observer=null;const refresh=()=>{observer?.disconnect();const nodes=[...document.querySelectorAll('.reveal')];if(motionReduced()||!('IntersectionObserver'in window)){nodes.forEach(x=>x.classList.add('is-visible'));return}observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('is-visible');observer.unobserve(e.target)}}),{rootMargin:'0px 0px -8% 0px',threshold:.08});nodes.forEach(x=>observer.observe(x))};reduced.addEventListener?.('change',refresh);addEventListener('hi:route-rendered',refresh);const settingsObserver=new MutationObserver(refresh);settingsObserver.observe(document.documentElement,{attributes:true,attributeFilter:['data-motion']});refresh();document.addEventListener('keydown',event=>{if(event.key==='/'&&!event.ctrlKey&&!event.metaKey&&!event.altKey&&!/INPUT|TEXTAREA|SELECT/.test(document.activeElement?.tagName||'')){event.preventDefault();document.querySelector('#global-search')?.focus()}if(event.key==='Escape'){document.querySelector('[data-modal][open]')?.close?.()}});return()=>{observer?.disconnect();settingsObserver.disconnect()}}
export function focusMain(){const main=document.querySelector('#main');main?.focus({preventScroll:true});scrollTo({top:0,behavior:motionReduced()?'auto':'smooth'})}
