const timeout=(ms)=>new Promise((_,reject)=>setTimeout(()=>reject(new Error('TIMEOUT')),ms));
const json=async(url,options={})=>{const res=await Promise.race([fetch(url,{...options,headers:{Accept:'application/json',...(options.headers||{})}}),timeout(7000)]);if(!res.ok){const e=new Error('HTTP_'+res.status);e.status=res.status;throw e}return res.json()};
export function createApi({apiBase='',catalogUrl}){const base=(apiBase||'').replace(/\/$/,'');let catalogPromise=null;const catalog=()=>catalogPromise||(catalogPromise=json(catalogUrl));const fromCatalog=async(kind)=>{const data=await catalog();if(kind==='categories')return {items:data.categories||[]};if(kind==='content')return {items:data.articles||[]};return {items:data[kind]||[]}};const get=async(path,fallbackKind)=>{if(base){try{return await json(base+path)}catch(error){console.warn('[Hogwarts Indonesia] API fallback',path,error.message)}}return fromCatalog(fallbackKind)};return{health:()=>base?json(base+'/api/v1/health'):Promise.resolve({status:'static-fallback'}),categories:()=>get('/api/v1/categories','categories'),content:(params={})=>{const q=new URLSearchParams(params);return get('/api/v1/content'+(q.size?'?'+q:''),'content')},contentBySlug:async slug=>{if(base){try{return await json(base+'/api/v1/content/'+encodeURIComponent(slug))}catch(error){console.warn('[Hogwarts Indonesia] detail fallback',error.message)}}const data=await catalog();return data.articles?.find(x=>x.slug===slug)||null},entities:(kind)=>get('/api/v1/'+kind,kind),timeline:()=>get('/api/v1/timeline','timeline'),glossary:()=>get('/api/v1/glossary','glossary'),daily:()=>get('/api/v1/daily-feature','daily_features'),search:async(params={})=>{if(base){try{const q=new URLSearchParams(params);return await json(base+'/api/v1/search?'+q)}catch(error){console.warn('[Hogwarts Indonesia] search fallback',error.message)}}const data=await catalog(),term=(params.q||'').toLowerCase().trim(),category=params.category||'';return{items:(data.articles||[]).filter(a=>(!category||a.category===category)&&(!term||[a.title,a.intro,a.slug,a.category].join(' ').toLowerCase().includes(term)))}}}}
