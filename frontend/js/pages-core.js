import {esc,collectionCards,articleCards,sectionHeading,sourceView} from './ui.js';
import {readList,toggleInList,pushRecent} from './state.js';

export function renderHome({main,categories,articles,progress,finish}){const featured=articles.slice(0,6);main.innerHTML=`<div class="route-frame"><section class="hero-castle" aria-labelledby="home-title"><div class="mist-layer"></div><div class="hero-copy"><span class="eyebrow">Ensiklopedia dunia sihir · Bahasa Indonesia</span><h1 id="home-title">Masuki kastil pengetahuan.</h1><p class="lede">Berjalan dari aula menuju perpustakaan, menara, rumah kaca, dungeon, dan arsip tersembunyi. Setiap ruang membuka hubungan baru antara tokoh, tempat, makhluk, mantra, dan peristiwa.</p><div class="hero-actions"><a class="button" href="#/index">Jelajahi perpustakaan →</a><a class="ghost-button" href="#/peta">Buka peta kastil</a></div></div><aside class="hero-statbar"><blockquote>Pengetahuan menjadi peta ketika setiap kisah terhubung dengan kisah lain.</blockquote><div class="stat-grid"><div><strong>${categories.length}</strong><span>Koleksi</span></div><div><strong>${articles.length}</strong><span>Artikel awal</span></div><div><strong>${progress}%</strong><span>Terjelajahi</span></div></div></aside></section>${sectionHeading('Great Hall','Dua belas pintu pengetahuan','#/index')}${collectionCards(categories)}${sectionHeading('Arsip pilihan','Mulai dari catatan yang telah diverifikasi','#/index')}${articleCards(featured,categories)}<section class="split" style="margin-top:3rem"><div><span class="eyebrow">Jalur alternatif</span><h2>Jelajahi berdasarkan ruang.</h2><p class="muted">Peta adalah navigasi editorial, bukan denah resmi. Timeline menata peristiwa, sementara glosarium membantu istilah kompleks tetap mudah dibaca.</p><div class="cluster"><a class="ghost-button" href="#/peta">Peta kastil</a><a class="ghost-button" href="#/timeline">Timeline</a><a class="ghost-button" href="#/glosarium">Glosarium</a></div></div><div class="wizard-note"><span class="eyebrow">Catatan editor</span><h3>Canon, sumber, dan tafsir dipisahkan.</h3><p>Artikel menandai fakta canon dan interpretasi editorial agar pembaca tidak perlu menebak mana informasi cerita dan mana pembacaan redaksi.</p></div></section></div>`;finish('Hogwarts Indonesia')}

export function renderIndex({main,categories,articles,category,params,finish,onRendered}){const initialQ=params.get('q')||'',initialCategory=category?.slug||params.get('category')||'';main.innerHTML=`<div class="route-frame"><span class="eyebrow">Restricted Section Index</span><h1>${esc(category?.title||'Perpustakaan')}</h1><p class="muted">Cari berdasarkan judul, istilah, lokasi, atau gagasan. Tekan <kbd>/</kbd> untuk menuju pencarian global.</p><form class="tools" id="library-search"><label class="field">Kata kunci<input name="q" type="search" value="${esc(initialQ)}" placeholder="Mis. Hogwarts, Patronus, Hogsmeade"></label><label class="field">Koleksi<select name="category"><option value="">Semua koleksi</option>${categories.map(c=>`<option value="${esc(c.slug)}" ${initialCategory===c.slug?'selected':''}>${esc(c.title)}</option>`).join('')}</select></label><button class="button">Cari arsip</button></form><p id="count" class="eyebrow" role="status"></p><div id="results"></div></div>`;const form=main.querySelector('#library-search'),results=main.querySelector('#results'),count=main.querySelector('#count');const update=()=>{const term=form.q.value.trim().toLowerCase(),cat=form.category.value;const list=articles.filter(a=>(!cat||a.category===cat)&&(!term||[a.title,a.intro,a.slug,a.category,...(a.sections||[]).map(s=>s.heading)].join(' ').toLowerCase().includes(term)));count.textContent=`${list.length} artikel ditemukan`;results.innerHTML=articleCards(list,categories);onRendered()};form.addEventListener('input',update);form.addEventListener('submit',e=>{e.preventDefault();location.hash='/index?'+new URLSearchParams(new FormData(form)).toString()});update();finish(category?.title||'Perpustakaan')}

export async function renderArticle({main,categories,articles,slug,api,finish,notice,onRendered,missing}){let a=articles.find(x=>x.slug===slug);if(!a)a=await api.contentBySlug(slug).catch(()=>null);if(!a)return missing();pushRecent(slug);const bookmarked=readList('bookmarks').includes(slug),c=categories.find(x=>x.slug===a.category);main.innerHTML=`<div class="route-frame"><p class="crumb"><a href="#/">Great Hall</a> / <a href="#/${esc(a.category)}">${esc(c?.title||a.category)}</a> / ${esc(a.title)}</p><div class="reading-layout"><article class="folio"><span class="eyebrow">${esc(a.canon||'Arsip Hogwarts Indonesia')}</span><h1>${esc(a.title)}</h1><p><strong>${esc(a.intro)}</strong></p>${(a.sections||[]).map((s,i)=>`${i===0?'<hr>':''}<h2>${esc(s.heading)}</h2><p>${esc(s.text)}</p>`).join('')}<aside><span class="eyebrow">Catatan Penyihir</span><p>${esc(a.note||'Catatan editorial belum tersedia.')}</p></aside><div class="tools"><button class="ghost-button" id="bookmark" aria-pressed="${bookmarked}">${bookmarked?'Hapus bookmark':'Simpan bookmark'}</button><button class="ghost-button" id="share">Bagikan</button></div></article><aside class="reading-aside stack"><div class="fact-box"><span class="eyebrow">Quick facts</span><dl><dt>Koleksi</dt><dd>${esc(c?.title||a.category)}</dd><dt>Status</dt><dd>${esc(a.status||'published')}</dd><dt>Provenance</dt><dd>${a.source?'Tersedia':'Perlu audit'}</dd></dl></div>${sourceView(a.source||{})}<div class="wizard-note"><span class="eyebrow">Navigasi</span><p><a href="#/${esc(a.category)}">Kembali ke koleksi</a></p><p><a href="#/sumber">Lihat daftar sumber</a></p></div></aside></div>${sectionHeading('Relasi','Baca berikutnya')}${articleCards(articles.filter(x=>(a.related||[]).includes(x.slug)),categories)}</div>`;main.querySelector('#bookmark').addEventListener('click',e=>{const on=toggleInList('bookmarks',slug);e.currentTarget.setAttribute('aria-pressed',String(on));e.currentTarget.textContent=on?'Hapus bookmark':'Simpan bookmark';notice(on?'Artikel disimpan pada perangkat ini.':'Bookmark dihapus.')});main.querySelector('#share').addEventListener('click',async()=>{try{if(navigator.share)await navigator.share({title:a.title,url:location.href});else{await navigator.clipboard.writeText(location.href);notice('Tautan artikel disalin.')}}catch(error){if(error.name!=='AbortError')notice('Salin alamat artikel dari bilah alamat browser.')}});finish(a.title);onRendered()}
