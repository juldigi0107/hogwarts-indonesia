import app from './index-v2.js';
export default {async fetch(request,env,ctx){const url=new URL(request.url);if(request.method==='GET'&&url.pathname==='/api/v1/astronomy'){const origin=request.headers.get('Origin');const allowed=(env.ALLOWED_ORIGINS||'').split(',').map(x=>x.trim()).filter(Boolean);if(origin&&!allowed.includes(origin))return new Response(JSON.stringify({error:{code:'ORIGIN_NOT_ALLOWED'}}),{status:403,headers:{'Content-Type':'application/json; charset=utf-8','Vary':'Origin'}});if(!env.DB)return new Response(JSON.stringify({error:{code:'DATABASE_NOT_CONFIGURED'}}),{status:503,headers:{'Content-Type':'application/json; charset=utf-8'}});try{const result=await env.DB.prepare('SELECT id,slug,name,type,summary,metadata FROM astronomy_entries ORDER BY id LIMIT 100').all();const items=(result.results||[]).map(row=>{try{row.metadata=JSON.parse(row.metadata)}catch{}return row});const headers={'Content-Type':'application/json; charset=utf-8','Cache-Control':'public, max-age=120, s-maxage=600','X-Content-Type-Options':'nosniff','Referrer-Policy':'no-referrer','Vary':'Origin'};if(origin)headers['Access-Control-Allow-Origin']=origin;return new Response(JSON.stringify({items}),{headers})}catch(error){console.error(JSON.stringify({event:'astronomy_api_error'}));return new Response(JSON.stringify({error:{code:'INTERNAL_ERROR'}}),{status:500,headers:{'Content-Type':'application/json; charset=utf-8'}})}}return app.fetch(request,env,ctx)}};
